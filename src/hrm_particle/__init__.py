"""HRM sequence-latent branching proof of concept.

The package deliberately keeps the actor reward (external verification) separate
from the learned Q selector.  See ``README.md`` for the staged experiment.
"""

__version__ = "0.1.0"

